language: objective-c
osx_image: xcode7.2
script:
- xctool -workspace Game.xcworkspace -scheme iOSGame -destination platform='iOS Simulator',OS=9.2,name='iPhone 6' build test -sdk iphonesimulator GCC_INSTRUMENT_PROGRAM_FLOW_ARCS=YES GCC_GENERATE_TEST_COVERAGE_FILES=YES
after_success:
- bash <(curl -s https://codecov.io/bash)